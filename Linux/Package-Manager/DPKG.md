---
создал заметку: 2025-09-29T01:10:00
author: WhiteK0T
tags:
  - DPKG
  - Пакетный_Менеджер
  - Linux
---

[**dpkg (Debian Package)**](https://help.ubuntu.ru/wiki/dpkg) — это низкоуровневый менеджер пакетов, который лежит в основе системы управления пакетами в Debian и производных дистрибутивах, таких как Ubuntu, Linux Mint и другие.
#### Основные функции dpkg
- **Установка пакетов** из локальных `.deb` файлов.
- **Удаление пакетов**.
- **Предоставление информации** о пакетах, например, к каким файлам они принадлежат.
- **Управление пакетами** на уровне системы без автоматического разрешения зависимостей (в отличие от [APT](APT.md)).
- **Сборка `.deb`-пакетов** из подготовленного каталога (`dpkg-deb --build`) — это упаковка готовых файлов, а не компиляция из исходного кода.
#### Особенности dpkg
- dpkg работает **только с локальными файлами пакетов** `.deb`, в то время как более высокоуровневые инструменты, такие как APT, умеют автоматически загружать пакеты из репозиториев и разрешать зависимости.
- dpkg управляется через командную строку с однокомандным синтаксисом: одна команда — одно действие с набором опций.
- dpkg не разрешает зависимости автоматически, поэтому при установке пакета вручную могут возникать проблемы с отсутствующими зависимостями, которые нужно устанавливать отдельно.
#### Примеры основных команд dpkg
- `dpkg -i package.deb` — установка пакета из файла.
- `dpkg -r package` — удаление установленного пакета.
- `dpkg -P package` — полное удаление пакета вместе с конфигурационными файлами.
- `dpkg -l` — список всех установленных пакетов.
- `dpkg -S /path/to/file` — поиск пакета, которому принадлежит файл.
- `dpkg -L package` — список файлов, установленных пакетом.

#### Зависимости и починка установки
- `sudo apt install -f` (или `apt-get -f install`) — дотянуть недостающие зависимости после `dpkg -i`. Самый частый приём: `dpkg` ставит `.deb`, но ругается на зависимости — эта команда их подтягивает из репозиториев.
- `dpkg --configure -a` — донастроить все распакованные, но ненастроенные пакеты. Чинит прерванную установку (`dpkg was interrupted`).

#### Информация о пакетах
- `dpkg -s <пакет>` — статус и метаданные **установленного** пакета.
- `dpkg -I package.deb` — control-данные (зависимости, описание) из **файла** `.deb` без установки.
- `dpkg -c package.deb` — список файлов внутри `.deb` без установки (что именно он положит в систему).
- `dpkg -V <пакет>` — проверка целостности установленного пакета: какие файлы изменились относительно исходных.

#### Бэкап и перенос набора пакетов
- `dpkg --get-selections > packages.txt` — выгрузить список установленных пакетов.
- `dpkg --set-selections < packages.txt` — восстановить отметки на другой машине (затем `sudo apt-get dselect-upgrade` доустановит их).

#### Сборка `.deb`
- `dpkg-deb --build <каталог>` — собрать `.deb` из подготовленного каталога (с `DEBIAN/control` внутри). Это упаковка готовых файлов, а не компиляция из исходного кода.

#DPKG 
#Пакетный_Менеджер 
#Linux 